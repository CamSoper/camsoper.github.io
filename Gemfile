source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Plugins (no gem theme — layouts are local)
group :jekyll_plugins do
  gem "jekyll-seo-tag"
end

# Windows / JRuby helpers (harmless on CI)
gem "webrick", "~> 1.8"
